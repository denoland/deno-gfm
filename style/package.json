{
  "name": "deno-gfm",
  "scripts": {
    "postinstall": "make patch",
    "build": "parcel build main.scss --no-source-maps"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.0",
    "@primer/css": "^18.0.1",
    "parcel": "^2.0.0"
  }
}
